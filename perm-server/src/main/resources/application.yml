server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/perm_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379

perm:
  jwt-secret: perm-server-secret-key-must-be-at-least-32-characters!!
  admin-password: admin123
  auto-scan: false
